import { LocationDropdownItemProps } from "@/(pages)/(publicRoutes)/(home)/components/LocationDropdown/types";

export const locationDropdownItemMock: LocationDropdownItemProps = {
  title: "Belo Horizonte",
  subtitle: "Belo Horizonte, MG",
  onClick: jest.fn(),
};
